language: python

python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
  - nightly

matrix:
  allow_failures:
    - python: nightly

services:
  - redis-server

install:
  - pip install -U pip setuptools
  - pip install -U -e ".[dev]"

script:
  - make test
